{
  "name": "realistic-good-claude-fixture",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "build": "tsc -p tsconfig.json",
    "ci": "pnpm typecheck && pnpm build && pnpm test"
  }
}
