# AGENTS.md

A minimal, intentionally incomplete instruction file used to exercise the
required-section rules. It only covers setup and test commands, leaving
the remaining sections missing on purpose.

## Setup commands

- Install dependencies: `pnpm install`

## Test commands

- Run tests: `pnpm test`
